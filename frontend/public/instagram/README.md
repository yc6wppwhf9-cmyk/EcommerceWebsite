# Instagram reel covers

Drop the four reel cover images here as:

- reel1.jpg
- reel2.jpg
- reel3.jpg
- reel4.jpg

Portrait covers, pre-cropped so the Instagram view-count bar is not included.
The #PriorityBags home section (`components/home/InstagramShowcase.tsx`) reads
these paths; if a file is missing it falls back to an on-site creative.
